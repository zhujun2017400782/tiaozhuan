<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zj.hello">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ManyAcyivity1"></activity>
        <activity android:name=".ManyActivity2"></activity>
        <activity android:name=".ManyActivity3"></activity>
        <activity android:name=".ManyActivity4"></activity>
        <activity android:name=".ManyActivity5"></activity>
        <activity android:name=".ManyActivity6"></activity>
        <activity android:name=".ManyActivity7"></activity>
    
    </application>

</manifest>